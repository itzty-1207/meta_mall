@import "./styles/iconfront.wxss";

/* 微信小程序中不支持通配符'*' */
page,view,text,swiper,swiper-item,image,navigator{
  padding: 0;
  margin:0;
  box-sizing: border-box;
}

/* 微信小程序默认支持css变量 */
page{
  /* 定义主题颜色 */
  --themeColor:#eead89;
  /* 定义统一字体大小 */
  font-size: 28rpx;
}
image{
  width: 100%;
}